{% extends 'base.html.twig' %}

{% block title %}Edit {{ user.fullName }} | FixFlow{% endblock %}
{% block topbar_title %}Edit User{% endblock %}

{% block body %}
    <div class="page-header">
        <div>
            <h1>Edit User</h1>
            <p class="muted">{{ user.fullName }} &middot; {{ user.email }}</p>
        </div>
        <a class="btn btn-secondary" href="{{ path('app_admin_user_index') }}">Back to Users</a>
    </div>

    <section class="panel">
        {{ include('admin/user/_form.html.twig', {'button_label': 'Update User'}) }}
    </section>
{% endblock %}