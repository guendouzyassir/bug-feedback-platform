{% extends 'base.html.twig' %}

{% block title %}New User | Bug Report Platform{% endblock %}

{% block body %}
    <div class="page-header">
        <div>
            <h1>New User</h1>
            <p class="muted">Create an account and assign the correct role.</p>
        </div>
        <a class="button button-secondary" href="{{ path('app_admin_user_index') }}">Back to Users</a>
    </div>

    <section class="panel">
        {{ include('admin/user/_form.html.twig', {'button_label': 'Create User'}) }}
    </section>
{% endblock %}
