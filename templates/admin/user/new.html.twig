{% extends 'base.html.twig' %}

{% block title %}New User | FixFlow{% endblock %}
{% block topbar_title %}New User{% endblock %}

{% block body %}
    <div class="page-header">
        <div>
            <p class="muted">Create an account and assign the correct role.</p>
        </div>
        <a class="btn btn-secondary" href="{{ path('app_admin_user_index') }}">Back to Users</a>
    </div>

    <section class="panel">
        {{ include('admin/user/_form.html.twig', {'button_label': 'Create User'}) }}
    </section>
{% endblock %}